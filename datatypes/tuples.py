myTuple = (1, 2 ,3)

print(dir(myTuple)) ## show all tuple methods.

## Tuple with a only value
x = (1,);                   print(type(x))  # return tuple

## Create tuple.
y = tuple((1, 2, 3));       print(y)        # return (1, 2, 3)